import { PlaceholderCard } from '../components/common/PlaceholderCard';
import { Screen } from '../components/layout/Screen';

export function BarsScreen() {
  return (
    <Screen>
      <PlaceholderCard
        title="Bars"
        description="A searchable list of bars, neighborhoods, and filter controls will be built here."
      />
    </Screen>
  );
}
