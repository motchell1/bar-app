import { PlaceholderCard } from '../components/common/PlaceholderCard';
import { Screen } from '../components/layout/Screen';

export function MapScreen() {
  return (
    <Screen>
      <PlaceholderCard
        title="Map"
        description="Map-first discovery with pins and proximity-based specials will live in this tab."
      />
    </Screen>
  );
}
